<div class="row ms-5">
    {{#each footer}}
    <div class="col-2 me-4 mb-5">
        <div class="nav flex-column">
            {{!-- <div {{#unless @first}} class="ms-4" {{/unless}}> --}}
                {{#with [0]}}
                <div class="nav-item">
                    <a href={{link}} class="nav-link active">{{text}}</a>
                </div>
                {{/with}}
                {{#with [1]}}
                <div class="nav-item">
                    <a href={{link}} class="nav-link active">{{text}}</a>
                </div>
                {{/with}}
                {{#with [2]}}
                <div class="nav-item">
                    <a href={{link}} class="nav-link active">{{text}}</a>
                </div>
                {{/with}}
                {{#with [3]}}
                <div class="nav-item">
                    <a href={{link}} class="nav-link disabled">{{text}}</a>
                </div>
                {{/with}}
                {{!--
            </div> --}}
        </div>
    </div>
    {{/each}}
</div>